(ns de.virtual-earth.superdesk-graphql.graphql-api.core-test
  (:require [clojure.test :as test :refer :all]
            [de.virtual-earth.superdesk-graphql.graphql-api.core :as core]))

(deftest dummy-test
  (is (= 1 1)))
