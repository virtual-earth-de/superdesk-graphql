(ns de.virtual-earth.superdesk-graphql.production-api.interface-test
  (:require [clojure.test :as test :refer :all]
            [de.virtual-earth.superdesk-graphql.production-api.interface :as production-api]))

(deftest dummy-test
  (is (= 1 1)))
