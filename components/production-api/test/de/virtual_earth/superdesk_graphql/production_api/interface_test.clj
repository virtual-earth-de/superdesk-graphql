(ns de.virtual-earth.superdesk-graphql.production-api-auth.interface-test
  (:require [clojure.test :as test :refer :all]
            [de.virtual-earth.superdesk-graphql.production-api-auth.interface :as production-api-auth]))

(deftest dummy-test
  (is (= 1 1)))
